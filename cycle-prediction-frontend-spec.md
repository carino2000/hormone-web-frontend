# 생리 주기 예측 앱 — 프론트엔드 설계 노트

> 이 문서는 데이터셋 실측 + 모델팀 확인 내용을 정리한 프론트엔드 작업 기준입니다.
> `[확정]` = 검증됨 / `[가정]` = 논의로 정한 방향 / `[미확정]` = 답변 대기

---

## 1. 데이터셋 실측 결과 `[확정]`

`merged_nan.csv` 를 직접 분석한 결과입니다.

| 항목 | 값 |
|---|---|
| 전체 행 | 5,436 |
| 참가자 수 | 40명 |
| 피처 수 | 55개 (웨어러블 + 인구통계) |
| 사람당 관측일 | 56 ~ 209일 (**90일 고정 아님**) |

- 참가자 20명은 `study_interval = 2022` 단일 구간
- 나머지 20명은 `2022` + `2024` 두 구간. `day_in_study` 가 1~1004까지 있지만 실제 관측일은 190일 내외 → **중간에 큰 공백 존재**
- 1행 = (참가자 1명 × 하루)

### 결측률 (프론트 설계에 직접 영향)

| 컬럼 | 결측률 |
|---|---|
| `steps`, `averageheartrate`, `exercise_calories`, `originalduration` | 69% |
| `pdg` | **65%** |
| `glucose_mean`, `glucose_std` | 44% |
| `sedentary` | 37% |
| `stress_score` | 33% |
| `lh`, `estrogen`, `phase` | 0% |

**결측은 예외 상태가 아니라 기본 상태입니다.** 빈 카드 / 끊어진 그래프 / 회색 처리된 날짜를 처음부터 컴포넌트로 만들어야 합니다.

### 주기 길이 편차

참가자 1번 실측: 1주기 26일, 2주기 32일. **같은 사람 안에서도 6일 차이.**
→ 28일 하드코딩 금지. 주기 길이는 항상 가변으로 다룰 것.

### 날짜 컬럼 없음

데이터셋에는 실제 날짜가 없고 `day_in_study`(연구 며칠째)와 `is_weekend` 만 있습니다.
→ 실제 캘린더 날짜는 **가입일 기준으로 프론트에서 계산**해야 합니다. (아래 5절 참고)

---

## 2. 모델 입출력 계약 `[확정 — 모델팀 확인]`

### 구조

```
[0 ~ 19일차]   콜드스타트. 데이터 수집만, 예측 없음
[20일차 이후]  매일 1일치 x 데이터 전송
                ├─ 출력 1: 그날의 pdg / estrogen / lh 추정치
                └─ 출력 2: 가장 가까운 월경 예정일
                            (20+n 일치 누적 데이터로 매일 재계산)
```

### 두 출력의 성격이 다름 — 화면에서 분리할 것

| | 출력 1 (호르몬) | 출력 2 (월경 예정일) |
|---|---|---|
| 대상 | 과거·현재 | 미래 |
| 갱신 | 한 번 나오면 고정 | **매일 흔들림** |
| UI 취급 | 기록/로그 성격 | 예측/불확실성 성격 |

같은 카드에 섞지 말 것.

---

## 3. 핵심 설계 문제: 예정일이 매일 바뀜

20 → 21 → 22일치로 재계산하므로 예측은 **반드시** 흔들립니다. 숨기지 말고 UI로 흡수합니다.

### 규칙 `[가정]`

1. **점이 아니라 범위로 표시** — "9월 12일 ~ 16일", 그 안에 최빈값 표기
2. **조용히 갈아끼우지 않음** — "어제보다 하루 늦춰졌어요" 명시
3. **최근 예측 변동폭을 신뢰도 대용 지표로 사용** — "최근 5일간 ±1일 내 안정적"

모델팀에 예측 구간/표준편차를 요청하되, 없으면 프론트에서 최근 7일 예측값의 분산으로 범위를 계산.

---

## 4. API 응답 스키마 (요청안)

```json
{
  "user_id": "u_1024",
  "collection_day": 34,
  "valid_days": 31,
  "status": "active",

  "hormones": {
    "date": "2026-09-03",
    "pdg": { "value": 4.1, "baseline_ratio": 0.9, "confidence": "low" },
    "estrogen": { "value": 226.3, "baseline_ratio": 1.4, "confidence": "high" },
    "lh": { "value": 1.2, "baseline_ratio": 1.0, "confidence": "high" }
  },

  "next_period": {
    "predicted_date": "2026-09-14",
    "range_start": "2026-09-12",
    "range_end": "2026-09-16",
    "shift_from_yesterday_days": 1,
    "recent_volatility_days": 1.0
  }
}
```

**중요:** `status` 는 문자열 하나로 화면 분기를 결정하는 필드.
`collecting` | `active` | `overdue` | `insufficient_data`

`baseline_ratio`(개인 평균 대비 비율)를 서버에서 계산해주면 좋지만, 20일치가 이미 있으므로 프론트에서 계산해도 됨.

---

## 5. 날짜 처리 `[가정]`

- **모델에게는 상대 순서**(`day_in_study`), **사용자에게는 절대 날짜**
- 두 레이어를 모두 유지하고, 변환은 프론트가 담당 (가입일만 저장하면 됨)
- 사용자는 "62일차"로 생각하지 않고 "9월 3일"로 생각함. 캘린더 멘탈 모델에서 벗어나면 안 됨
- 절대 날짜가 반드시 필요한 기능:
  - 다음 월경 예상일 표시
  - 앱 미접속 시 "이 예측은 며칠 전 것인가" 판단
  - 수면 데이터의 자정 경계 / 타임존 처리 → **경계 규칙을 백엔드와 합의 필요**

---

## 6. 온보딩 (콜드스타트) `[가정]`

모델 게이트는 20일이지만, 주기 편차를 고려해 3단계로 분리 권장.

| 단계 | 조건 | 화면 |
|---|---|---|
| 기록만 | 0 ~ 19일 | 예측 없음. 수집 진행률 + 웨어러블 데이터 시각화만 |
| 잠정 예측 | 20 ~ 35일 | 표시하되 "추정" 라벨 + 낮은 신뢰도 명시 |
| 안정 | 1주기 이상 확인 후 | 정상 예측 |

**진행률은 유효일 기준으로 카운트할 것.** 웨어러블 미착용일이 섞이면 사용자가 20일이 언제 채워지는지 알 수 없음.

---

## 7. 필수 화면 상태 목록

컴포넌트 단위로 전부 만들어둘 것.

### 공통
- [ ] `collecting` — 수집 중 (n/20일, 유효일 기준)
- [ ] `insufficient_data` — 결측 과다로 이번 구간 예측 불가
- [ ] 저신뢰 — 값은 있으나 불확실. "추정" 라벨 + 시각적 약화

### 월경 예정일 카드
- [ ] 정상 — 범위 + 최빈값 + 변동 안내
- [ ] 불안정 — 최근 예측이 크게 출렁임. 범위를 넓게, 경고 문구
- [ ] `overdue` — **예상일이 지났는데 기록 없음.** 과거 날짜를 계속 가리키면 안 됨. "예상일이 지났어요, 기록해주세요"로 전환

### 호르몬 카드
- [ ] 정상 3종
- [ ] `pdg` 저신뢰 처리 (학습 데이터 65% 결측 → 다른 둘보다 신뢰도 낮음, 시각적으로 약화)
- [ ] 결측일 — 값 대신 `—`

### 타임라인
- [ ] 관측 없는 날은 이어붙이지 말고 **점선/빈칸으로 구멍 표시**

---

## 8. UI 원칙

1. **호르몬 원시값을 그대로 노출하지 않는다.** "에스트로겐 226.3"은 일반 사용자에게 의미 없음. 개인 기준선 대비로 번역 ("평소보다 높음").
2. **주기 길이 28일 하드코딩 금지.**
3. **불확실성을 숨기지 않는다.** 정직한 범위 표시가 틀린 확신보다 신뢰를 얻음.
4. **결측을 정상 상태로 디자인한다.**

---

## 9. 안전 관련 `[중요]`

- **피임·임신 시도 목적으로 사용하지 말 것**을 눈에 띄는 위치에 안내. 가임기 정보를 화면에 띄우는 순간 사용자는 그렇게 사용함.
- **의료 진단이 아닌 추정치**임을 명시.
- 학습 표본이 40명으로 매우 작음. 개인차가 큰 도메인이라 일반화 성능에 한계가 있을 수 있음 → 확신에 찬 단정형 문구 피할 것.

---

## 10. 미확정 — 모델팀 확인 필요 `[미확정]`

- [ ] **`phase` 는 출력에 포함되는가?** 데이터셋에는 4단계(월경기/난포기/가임기/황체기)가 있으나 모델팀 설명에는 호르몬 3종 + 예정일만 언급됨.
      → 출력에 없다면 "오늘의 단계" 카드는 근거 없음. **호르몬 수치에서 프론트가 역산하는 것은 임상적으로 위험하므로 금지.**
- [ ] 월경 예정일이 **날짜**로 오는가, **며칠 뒤**로 오는가?
- [ ] 예측 신뢰구간 / 표준편차를 함께 줄 수 있는가?
- [ ] 사용자의 실제 월경 시작 자가입력이 모델에 피드백되는가?
      (파일명은 `hormones_and_selfreport.csv` 이지만 실제 self-report 컬럼은 없음)
- [ ] `id`(참가자 번호)가 학습 피처에 포함되었는가?
      → 포함되었다면 **학습에 없던 새 사용자에게 예측 불가.** 실서비스라면 치명적.
      검증을 참가자 단위로 분리했는지도 함께 확인 (같은 사람이 train/test에 동시 존재하면 성능이 부풀려짐).
- [ ] 20일 게이트 중 결측일이 많으면 어떻게 처리되는가? (유효일 기준 재계산 여부)

---

## 11. 다음 작업 후보

- 20일 온보딩 플로우 화면
- 월경 예정일 카드의 상태별 변형 (지연 / 불안정 / 데이터 부족)
- 캘린더 뷰 (결측일 구멍 표시 포함)
